<div class="school">
  <!-- 分类 -->
  <div class="container">
    <div class="types">
      <div class="typeTop">
        <!-- 行业类型开始 -->
        <AreaSelection
          :title="typeIndustryTitle"
          :dataArr="typeIndustry"
          @itemEvent="typeIndustryItemClick"
        >
        </AreaSelection>
        <!-- 行业类型结束 -->

        <!-- 地区开始 -->
        <AreaSelection
          :title="typeaddTitle"
          :dataArr="typeAdd"
          @itemEvent="typeAddItemClick"
        >
        </AreaSelection>
        <!-- 地区结束 -->
      </div>
      <SearchTab
        :title="filterTitle"
        :optionList="filterList"
        :defaultId="filterList[0].id"
        :searchPlaceholder="'搜索学校名称'"
        :isShowIdentify="false"
      >
      </SearchTab>
    </div>
    <div class="LIST">
      <div
        class="item"
        v-for="(item,index) in enterLIST"
        :key="index"
      >
        <ItemBar
          :item="item"
        >
<<<<<<< HEAD
          <div
            @click="details(item.id)"
            class="LS-Top"
          >
            <div>
              <div class="one"></div>
            </div>
            <div>
              <div
                class="tu clearfix"
                v-if="item.certificationFlag"
              ><img
                  src="../../img/authentication.png"
                  alt=""
                ></div>
              <div class="two">{{item.schoolname}}</div>
              <div class="three">{{item.introduction}}</div>
            </div>
          </div>
          <div class="four">
            <div>
              <div>学徒岗位</div>
              <div>{{item.xtSpecialtyCount}}</div>
            </div>
            <div>
              <div>企业师傅</div>
              <div>{{item.xtTutorCount}}</div>
            </div>
          </div>
        </div>

=======
        </ItemBar>
>>>>>>> 0d79ce80398e9bdde1d11dfb10357b3ea0525ed8
      </div>

      <!--</div>-->
      <!--<div class="block">-->
      <!--<div>-->
      <!--<el-pagination-->
      <!--@size-change="handleSizeChange"-->
      <!--@current-change="handleCurrentChange"-->
      <!--:current-page.sync="currentPage3"-->
      <!--:page-size="100"-->
      <!--layout="prev, pager, next, jumper"-->
      <!--:total="1000"-->
      <!--&gt;-->
      <!--</el-pagination>-->
      <!--</div>-->
      <!--</div>-->
    </div>

  </div>
</div>
